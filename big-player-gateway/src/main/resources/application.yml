spring:
  application:
    name: @application.name@
  config:
    import: classpath:base.yml,classpath:cache.yml,classpath:config.yml
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin, RETAIN_UNIQUE
      globalcors:
        cors-configurations:
          '[/**]':
            allowedHeaders: '*'
            allowedMethods: '*'
            allowedOrigins: '*'
      routes:
        - id: big-player-auth
          uri: lb://big-player-auth
          predicates:
            - Path=/auth/**
        - id: big-player-business
          uri: lb://big-player-business
          predicates:
            - Path=/user/**
server:
  port: 8081
