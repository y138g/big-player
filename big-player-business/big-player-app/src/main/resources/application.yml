spring:
  profiles:
    active: dev
  application:
    name: @application.name@
  config:
    import: classpath:base.yml,classpath:datasource.yml,classpath:rpc.yml,classpath:cache.yml,classpath:limiter.yml

server:
  port: 8085

dubbo:
  application:
    qos-port: 33334
